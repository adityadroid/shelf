# Shelf

Shelf is an offline-first macOS document search app focused on local PDF, DOC, and DOCX libraries.

## Development

Create the environment and install dependencies with `uv`:

```bash
uv sync
```

Run the app:

```bash
uv run shelf
```

Run tests:

```bash
uv run pytest
```
